name: CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-test:
    name: Continuous Integration
    uses: bcrutherford/reusable-workflows/github/workflows/reusable-build.yml@main
    with:
      dotnet_version: 9.0.x
